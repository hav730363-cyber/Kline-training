# 公网试用部署说明

当前项目已经按公网 Web App 方式准备：`server.py` 会读取平台提供的 `PORT`，并支持 `HOST=0.0.0.0`；`/health` 可用于托管平台健康检查。

## 推荐发布流程

1. 将当前目录上传到自己的 GitHub 仓库。
2. 在 Render 新建 Web Service，选择这个仓库。
3. 使用项目中的 `render.yaml`，或手动填写：
   - Build Command：`pip install -r requirements.txt`
   - Start Command：`python server.py`
4. 发布完成后，平台会生成一个公网网址，发给测试人员即可。

## 试用阶段边界

- 用户名、训练记录、候选审核状态仍保存在各自浏览器，不会互相共享。
- 真实行情接口由服务端代为请求；正式公开前应确认数据源授权和访问限制。
- 当前没有密码登录、权限控制和多人 PK，暂时只适合小范围功能试用。
- 不要把个人密钥、账号密码或私有数据提交到 GitHub。
