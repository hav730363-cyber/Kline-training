# K 线形态训练 App：产品需求说明书 v1

## 1. 产品目标

为用户提供一个面向中国 A 股和场内 ETF 的历史行情模拟训练平台，用于练习裸 K 盘感、识别指定 K 线形态、执行分仓交易，并通过单人训练和多人 PK 检验交易纪律与结果。

本产品仅用于模拟训练，不连接真实证券账户，不执行真实交易。

## 2. 已确认范围

- 标的：中国 A 股、场内 ETF
- 交易方向：只做多，不做空，不使用杠杆
- 初始本金：1,000,000 元
- 支持分仓、加仓、减仓、清仓
- 买卖按训练规则成交，不连接真实账户
- 正常日线模式：支持 A 股式交易规则
- ETF 分时模式：5 分钟 K 线、单个交易日、T+0
- 裸 K 训练：使用半年内历史波段
- 日常 PK：使用一年历史行情
- PK 房间：2—10 人
- 注册方式：输入昵称即可；系统另生成匿名用户 ID
- 样本库、训练库分离，并允许用户选择抽取来源
- 指标仅供参考，不强制生成买卖信号
- 排除 ST、停牌、退市风险和不符合流动性要求的标的

## 3. 交易规则

### 3.1 日线模式

- 底层数据为日线 OHLCV。
- 支持日、周、月、年周期显示和指标计算。
- 用户在当前 K 线结束后下单，成交价为当前 K 线收盘价。
- A 股默认 T+1：当日买入的股票下一交易日才可卖出。
- 普通 A 股按 100 股整数倍交易。
- ETF 交易单位、T+0/T+1 规则通过品种配置控制。
- 涨停时禁止买入，跌停时禁止卖出；停牌时禁止交易。
- 允许全部持币，不强制持仓。
- 场景结束时，未卖出的持仓按最后一根 K 线收盘价计入总资产。

### 3.2 分时 T+0 模式

- 使用 5 分钟 K 线。
- 每局先实现单个交易日，不跨夜持仓。
- ETF 当日买入后允许当日卖出。
- 用户逐根推进 K 线并进行交易。
- 成交价、交易时间、手续费和可用资金由服务器统一计算。
- 后续可扩展为多日分时训练，但不纳入第一版范围。

### 3.3 费用和限制

- 手续费按真实市场规则建模。
- 由于券商费率和市场规则可能变化，手续费、印花税、过户费、最低收费和交易单位均为可配置项。
- 每次交易保存使用的费用规则版本，保证历史复盘结果可重现。
- 正式接入数据前，实际费率和涨跌停规则需再次核验。

## 4. 功能模块

### 4.1 行情与图表

- 蜡烛 K 线、成交量、十字光标和缩放。
- 指标：KDJ、MACD、成交量、MA5/10/20/30/60/120/250。
- 支持日、周、月、年周期。
- 交易点在 K 线上标注买入、卖出、加仓、减仓位置。
- 训练过程中支持隐藏名称、代码、日期等身份信息。

### 4.2 模拟交易

- 买入、卖出、加仓、减仓、清仓。
- 按金额或股数下单。
- 显示现金、持仓数量、持仓成本、浮动盈亏、已实现盈亏和总资产。
- 交易前校验资金、持仓、可卖数量、涨跌停和 T+1 限制。
- 每笔交易记录价格、数量、费用、成交状态和用户备注。

### 4.3 裸 K 练盘感

- 从符合筛选条件的标的和历史波段中随机抽取场景。
- 默认使用半年内波段。
- 每次只展示已到达的 K 线，用户点击“下一根 K 线”推进。
- 训练时隐藏股票名称、代码和日期。
- 结束后显示真实标的、日期、完整走势、指标和交易复盘。
- 统计收益率、胜率、最大回撤、交易次数和资金曲线。

### 4.4 形态训练

首批形态来自用户上传的图片，包括三重底/顶、三角形、楔形、旗形、矩形、圆底/顶、头肩底/顶、岛型反转、钻石底/顶、Adam&Eve 双底/双顶等。

形态系统分为两层：

- 样本库：程序筛选或导入的候选波段，允许未审核。
- 训练库：人工确认后的正式训练样本。

每个样本保存形态名称、方向、周期、起止位置、规则版本、审核状态和数据版本。第一版采用规则筛选加人工审核，不直接依赖 AI 自动识别。

### 4.5 多人 PK

- 创建房间并生成房间码。
- 玩家输入昵称和房间码加入。
- 房主开始后，服务器为所有玩家分配相同标的、相同起点和相同结束点。
- 所有玩家同步推进 K 线，但各自独立交易。
- 服务器只向客户端发送当前可见行情，避免提前泄露未来数据。
- 默认按结束时总资产排名，同时展示收益率、最大回撤和交易次数。
- 结束后显示个人交易记录和全体排名。

## 5. 数据设计原则

- 保存原始行情、复权行情和复权因子。
- 保留数据来源、更新时间、数据版本和校验状态。
- 训练场景创建后固定数据版本，避免行情更新导致同一场景结果变化。
- 每日收盘后自动更新日线数据并记录成功或失败日志。
- 分时模式使用独立的 5 分钟数据源和缓存策略。
- 数据源的 API 条款必须允许当前使用方式；正式部署前需确认授权范围。

## 6. 开发阶段

### 阶段一：单人日线 MVP

K 线、指标、模拟交易、资金持仓、交易记录、盈亏统计。

### 阶段二：裸 K 训练

随机波段、下一根 K 线、隐藏身份、结束复盘和训练统计。

### 阶段三：形态样本与训练库

形态规则、候选筛选、人工审核、样本库/训练库选择和形态统计。

### 阶段四：5 分钟 T+0 单日训练

5 分钟数据、单日回放、T+0 交易和分时复盘。

### 阶段五：多人远程 PK

账号匿名 ID、房间码、实时同步、服务器结算、排名和复盘。

## 7. 第一版验收标准

- 能选择或随机抽取 A 股/场内 ETF 历史日线。
- 能逐根查看 K 线，并按规则完成买卖。
- 资金、持仓、手续费和盈亏计算一致。
- 买卖点能准确显示在 K 线上。
- KDJ、MACD、成交量和均线能切换周期。
- 裸 K 模式不会提前显示名称、代码、日期和未来 K 线。
- 训练结束后能查看完整复盘和交易明细。
- 同一场景重复打开时结果可重现。

## 8. 当前默认决策

如果后续没有特别说明，开发按以下默认值执行：

- 日线裸 K：半年历史波段。
- 日线 PK：一年历史行情。
- 分时训练：5 分钟 K 线、单个交易日、ETF T+0。
- PK 人数：2—10 人。
- 排名：最终总资产优先，最大回撤作为辅助指标。
- 注册：昵称加系统匿名 ID，无需手机号和密码。
- 第一版：先完成单人日线 MVP。
