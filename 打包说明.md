# K线训练打包说明

当前版本提供两种打包方式：

## 手机端 PWA

网页部署到 HTTPS 公网地址后，用手机浏览器打开，在浏览器菜单中选择“添加到主屏幕”或“安装应用”。PWA 会以独立窗口运行，并缓存页面外壳；真实行情接口仍需要联网。

## Windows EXE

本项目已提供 `K线训练.spec` 和 `desktop_launcher.py`。在已安装 Python 的 Windows 电脑上执行：

```powershell
python -m pip install pyinstaller
pyinstaller --clean --noconfirm "K线训练.spec"
```

生成的文件位于 `dist\K线训练.exe`。双击后会自动启动本地服务并打开浏览器页面，桌面版默认使用 18000-18049 端口范围，不会与开发服务器的 8000 端口冲突。第一次获取真实行情时需要联网。

本轮优化已生成并行新版：`dist-next\K线训练.exe`，以及可直接复制给他人的 `K线训练-Windows-新版.zip`。由于旧版 EXE 当时仍在运行，未强制覆盖旧文件。

当前推荐给 Windows 用户的版本是：`dist-browser\K线训练-浏览器版.exe`。它会先确认本地服务已启动，再自动打开默认浏览器，避免出现 `ERR_EMPTY_RESPONSE`。双击即可使用，不需要手动输入 `127.0.0.1`。

给他人分发时使用：`K线训练-Windows-浏览器版.zip`。解压后双击其中的 EXE；如果 Windows 防火墙询问网络权限，请允许专用网络访问。

## 更新方式

PWA 更新取决于服务器上的网页文件；EXE 更新需要重新打包并替换旧文件。多人联机和共享数据仍需要公网后端服务，本地 EXE 不能单独提供多人房间。
