# Kline Training 项目成果与后续开发参考

> 最后整理：2026-09-11  
> 项目：K 线形态训练 Web App / Windows 单机版  
> 当前阶段：单人日线训练 MVP 已完成第 1—11 步基础优化；多人远程 PK 暂缓

## 1. 文档用途

本文档是本项目的工作总览，供后续开发、调试、打包和需求讨论时优先参考。

后续继续开发时应遵守以下原则：

1. 不重复实现本文档中已经完成的功能。
2. 真实行情必须来自东方财富；接口失败时提示失败，不得用虚假或合成数据冒充真实行情。
3. 样本库和正式训练库分离；未经审核的候选样本不得进入随机训练池。
4. 每个阶段完成后都要执行语法、接口、核心流程和交易边界调试。
5. 涉及交易口径、数据授权、自动入库和多人共享的高风险取舍，先列出问题再确认。

## 2. 产品目标与已确认范围

这是一个用于练习盘感、K 线形态识别和模拟交易纪律的训练工具，不连接真实证券账户，也不执行真实交易。

### 已确认的产品范围

- 市场：中国 A 股和场内 ETF。
- 方向：只做多，不做空，不使用杠杆。
- 初始本金：1,000,000 元。
- 交易：支持分仓、加仓、减仓、清仓。
- 成交：训练中按当前 K 线收盘价成交。
- 默认周期：日线；页面也支持周、月、年周期读取。
- A 股：按 T+1 处理，当日买入的数量下一交易日才可卖出。
- ETF：支持当前页面的 T+1 和 T+0 训练规则选项。
- 裸 K 训练：训练过程中隐藏标的身份和日期，结束后揭晓。
- 形态：来自用户提供的 5 张参考图片，已整理为 40 个形态模板。
- 用户：当前为自用测试，输入昵称即可登录；不提供密码和账号安全体系。
- 多人 PK：产品需求已确定为 2—10 人房间码联机，但当前阶段明确后置。

## 3. 第 1—11 步交付结果

### 第 1 步：单人日线训练基础

- 响应式电脑/手机页面。
- 逐根推进 K 线。
- 训练状态、现金、持仓、成本、浮盈亏和总资产实时更新。
- 训练结束后可以查看完整走势和结果。

### 第 2 步：模拟交易与分仓

- 买入、卖出、加仓、减仓和清仓。
- 100 股整数倍下单。
- 买入和卖出均支持：全仓、半仓、三分之一仓、六分之一仓。
- 快捷仓位按钮只填写数量，不会绕过确认直接成交。
- 买入时会预留手续费，避免全仓后因费用不足失败。

### 第 3 步：快捷推进

- “下一根 K 线”按钮。
- 空格键推进下一根 K 线。
- 长按空格不会连续误推进。
- 输入框、下拉框、按钮和可编辑区域获得焦点时，空格键不会触发推进。

### 第 4 步：指标与图表

- 主图：蜡烛 K 线、MA5/MA20/MA60、交易买卖标记。
- 成交量附图：成交量柱、VOL-MA120、VOL-MA250。
- 缩放重绘已增加画布尺寸兜底、变换矩阵重置和布局完成后的下一帧重绘，避免缩放到较小窗口后 K 线消失。
- 量能提示：天量、地量、明显放量、明显缩量和正常量能。
- MACD 附图：DIF、DEA、红绿柱。
- KDJ 附图：K、D、J 三条线。
- 指标仅供观察，不自动生成买卖信号，也不替代个人判断。
- 支持日、周、月、年行情周期选择。

### 第 5 步：筹码峰与形态参考

- 主图右侧加入估算筹码分布。
- 显示主筹码峰、估算平均成本、获利比例和 70% 筹码区间。
- 筹码峰可单独开关，关闭后主图恢复完整宽度。
- 40 个形态名称后均配有简化 SVG 走势示意图，便于快速参考。
- 筹码峰是量价估算，不是真实券商筹码数据；当前不能用于自动买卖判断。

### 第 6 步：形态模板库

- 样本库与训练库分离。
- 可按形态类别和名称筛选。
- 可随机抽取形态知识模板。
- 知识模板只用于学习形态规则，不会改变当前行情或训练答案。
- 支持将模板加入或移出本机训练库。
- 训练结束后，对带标签的样本揭晓形态答案和完整走势。

### 第 7 步：候选片段与审核流程

- 真实行情或本地 CSV 可扫描候选底部片段。
- 支持 60、90、120 根 K 线窗口。
- 初筛参考下跌、修复、底部位置、量能收缩、反弹动能和形态结构。
- 候选结果包含标的、起止日期、形态、初筛评分和审核状态。
- 支持“采用此形态”“训练此片段”“审核通过”和“驳回”。
- 候选可载入为独立训练区间，不必使用整段行情。
- 当前评分是启发式初筛分数，不等于真实概率；未完成回测前不会自动入正式训练库。

### 第 8 步：真实行情接入

- 服务端已从原来的演示行情接口迁移到东方财富行情接口。
- 支持输入 6 位 A 股或场内 ETF 代码。
- 支持东方财富前复权（`qfq`），服务端也保留后复权和不复权参数能力。
- 请求周期：`daily`、`weekly`、`monthly`、`yearly`。
- 服务端同源代理用于避免浏览器跨域问题。
- 真实行情返回数据源、复权方式、周期、原始数量、有效数量、异常/重复数量和获取时间。
- 接口默认按需获取并缓存约 300 秒，不是后台持续请求。
- 真实行情不足 70 根有效 K 线时拒绝建立训练局。

### 第 9 步：交易规则和费用

当前前端训练规则版本：

`中国市场训练费率-v1（佣金0.03%最低5元；A股卖出印花税0.05%；过户费0.001%）`

- 佣金：成交金额 × 0.03%，最低 5 元。
- A 股卖出印花税：成交金额 × 0.05%。
- 过户费：成交金额 × 0.001%。
- A 股 T+1：当日买入不可当日卖出。
- ETF T+1：按 T+1 处理。
- ETF T+0：当根 K 线买入后允许卖出。
- 普通交易只允许做多，暂不支持融资、融券、杠杆和做空。
- 检查停牌、零成交量、ST/退市风险标的和涨跌停封板。
- 每笔交易记录费用明细和费用规则版本，便于复盘重现。

> 费用模型用于训练模拟，正式上线前仍需根据实际券商费率、交易所规则和数据源授权重新核验。

### 第 10 步：结束训练与复盘

- 可随时点击“结束训练”，不必推进到最后一根 K 线。
- 结束后自动显示完整训练区间，便于观察大局。
- 结果统计包括：收益率、最大回撤、胜率、交易次数、手续费和已实现盈亏。
- K 线上保留买卖点，交易记录保留成交价格、数量、费用和成交时点。
- 未卖出持仓按最后一根 K 线收盘价计入最终资产。

### 第 11 步：保存、样本库和可运行版本

- 未完成训练自动保存为浏览器本地草稿。
- 支持 JSON 训练备份导出和导入。
- 用户名、形态训练库、候选审核状态和收藏记录当前保存在本机浏览器。
- 已生成真实样本候选库 `sample_bank.json`。
- 已生成新版 Windows EXE 和压缩包。
- 已保留 PWA 文件，可在公网 HTTPS 页面中添加到手机主屏幕。
- 已提供 Render 公网部署配置 `render.yaml`。

## 4. 40 个形态模板

形态名称、类别、方向、规则和示意图均维护在 `library.js`。当前包含：

### 底部反转

三升谷底、三重底、喇叭底、岛型反转底、管形底、矩形底、圆底、钻石底、Adam&Adam 双底、Adam&Eve 双底、Eve&Adam 双底、Eve&Eve 双底、头肩底、复合头肩底。

### 顶部反转

三重顶、复合头肩顶、喇叭顶、岛型反转顶、管形顶、矩形顶、圆顶、钻石顶、Adam&Adam 双顶、Adam&Eve 双顶、Eve&Adam 双顶、Eve&Eve 双顶、头肩顶。

### 中继整理与趋势结构

上升三角形、下降三角形、对称三角形、下降楔形、上升楔形、规则上升、规则下降、三角旗形、四边旗形、高位紧密旗形、上升扇贝形。

### 其他结构

长岛型、缺口。

图片来源仅作为形态概念和示意参考，不代表严格、唯一或具有确定收益率的量化识别规则。

## 5. 当前真实样本候选库

文件：`sample_bank.json`

当前统计：

- 数据源：东方财富。
- 复权：前复权（`qfq`）。
- 周期：日线。
- 形态数：40。
- 每种形态：3 条。
- 候选总数：120 条。
- 每条样本：包含真实 OHLCV 快照、证券代码、起止日期、形态标签、数据源、复权方式、周期和审核状态。
- 当前审核状态：120 条全部为“待审核”。
- `autoEligible`：全部为 `false`。
- 当前样本库是候选样本库，不等于已审核正式训练库。

样本库建立原则：

1. 只有东方财富返回并通过 OHLCV、日期、价格和成交量校验的数据才可写入。
2. 不使用合成行情补缺。
3. 启发式评分不能直接解释为 90% 概率。
4. 只有人工审核通过，或后续有经过回测验证的识别规则，样本才可进入随机训练池。
5. 部分上游名称获取失败时，界面可能仅显示证券代码；这不影响行情数据来源真实性。

## 6. 数据与代码结构

| 文件 | 用途 |
| --- | --- |
| `index.html` | 页面结构、控件、指标区、交易区、样本库和复盘区 |
| `styles.css` | 响应式布局、图表、手机端样式和交互状态 |
| `app.js` | 状态管理、K 线绘制、指标计算、交易、训练、备份和样本逻辑 |
| `library.js` | 40 个形态模板、规则文字和 SVG 示意图 |
| `server.py` | 静态文件服务、健康检查和东方财富同源行情代理 |
| `sample_bank.json` | 40×3 的东方财富前复权真实候选样本快照 |
| `build_sample_bank.py` | 从东方财富获取并校验候选样本的采集脚本 |
| `desktop_launcher.py` | Windows EXE 启动器，启动本地服务并打开浏览器 |
| `K线训练.spec` | 原版 PyInstaller 打包配置 |
| `K线训练-浏览器版.spec` | 推荐浏览器版 PyInstaller 配置，已内置 `sample_bank.json` |
| `manifest.webmanifest` | PWA 应用清单 |
| `sw.js` | PWA 页面外壳缓存 |
| `icon.svg` | PWA/桌面版图标 |
| `render.yaml` | Render 公网 Web Service 配置 |
| `启动K线训练.cmd` | Windows 双击启动脚本 |
| `README.md` | 简明项目说明 |
| `产品需求说明书-v1.md` | 已确认的完整产品需求和阶段规划 |
| `测试记录.md` | 各阶段测试、问题和修复记录 |
| `阶段优化建议.md` | 已提出但尚未全部实施的优化建议 |
| `打包说明.md` | 手机端、PWA 和 Windows 打包说明 |
| `公网部署说明.md` | 公网部署步骤及限制 |

### 微信小程序测试版（已建立骨架）

- AppID：`wx709f67396b06ba71`。
- 原生小程序目录：`miniprogram/`。
- CloudBase 云函数目录：`cloudfunctions/getMarketData/`。
- 当前只验证微信身份、CloudBase 调用和东方财富真实行情链路，尚未声称完整小程序功能已迁移。
- 测试步骤和费用注意事项见 `小程序测试说明.md`。

## 7. 本地运行与打包

### 开发模式

在项目目录执行：

```powershell
python server.py
```

然后打开：`http://localhost:8000/`

如果 8000 端口被旧进程占用，应先关闭旧程序，或指定其他端口：

```powershell
$env:PORT = "8013"
python server.py
```

### Windows 版

当前新版文件：

- `dist-next\\K线训练.exe`
- `K线训练-Windows-新版.zip`
- `dist-browser\\K线训练-浏览器版.exe`（已修复无控制台启动时的空响应问题，优先使用）
- `K线训练-Windows-浏览器版.zip`（给他人使用的简化包）

双击浏览器版 EXE 后会先启动本地服务，确认端口可用后自动打开默认浏览器。桌面版使用 18000—18049 端口范围，第一次读取真实行情需要联网。不需要手动输入 `127.0.0.1`。

如果浏览器显示 `ERR_EMPTY_RESPONSE`，通常是旧程序进程仍占用端口或旧版 EXE 未正常启动；关闭旧版进程后，改用 `K线训练-Windows-浏览器版.zip` 中的新 EXE。

重新打包命令：

```powershell
pyinstaller --noconfirm --clean --distpath dist-next --workpath build-next "K线训练.spec"
```

### 手机端

- 局域网测试：电脑运行 `启动手机端.cmd`，手机和电脑连接同一 Wi-Fi，然后访问命令窗口显示的局域网地址。
- PWA：将 Web App 部署到 HTTPS 公网地址，手机浏览器选择“添加到主屏幕”或“安装应用”。
- PWA 只缓存页面外壳；真实行情仍需要联网访问服务端代理。

## 8. 公网分享部署边界

项目可以部署成可分享的网址，但需要能运行 Python 服务端的公网平台，例如 Render。推荐方式：

1. 将项目文件上传到 GitHub 仓库。
2. 在 Render 创建 Web Service，连接该仓库。
3. 使用 `render.yaml`，或填写：
   - Build Command：`pip install -r requirements.txt`
   - Start Command：`python server.py`
4. 发布后使用平台生成的 HTTPS 地址分享给其他人。

重要区别：

- GitHub Pages 只能托管静态文件，适合页面演示和内置样本库。
- GitHub Pages 本身不能运行 `server.py`，因此不能直接提供东方财富行情代理。
- 若要让分享者获取真实行情，需要 Render 等能运行 Python 服务的后端平台。
- 当前用户名、训练记录、候选审核和收藏仍保存在每个人自己的浏览器，不会跨用户共享。
- 当前没有密码登录、权限控制、公共审核后台和多人 PK。
- 正式公开前必须确认东方财富接口的授权范围、访问频率和公开服务条款。

## 9. 已完成的验证

最近一次回归验证已通过：

- Python 语法检查：`server.py`、`desktop_launcher.py`、`build_sample_bank.py`。
- JavaScript 语法检查：`app.js`、`library.js`。
- HTML 控件 ID 重复检查：通过。
- `git diff --check`：通过。
- 本地 `/health`：HTTP 200。
- 非法周期和非法代码：返回明确的 HTTP 400，不覆盖已有行情。
- 东方财富响应解析：OHLCV、复权、周期、数量和更新时间元数据正确。
- 样本库校验：120 条、40 种形态、每种 3 条、无无效 K 线。
- 新版 EXE：启动成功，`/health` 返回 200，内置样本库可访问，报告 40 种形态和 120 条样本。
- 交易边界：资金不足、非整数手、T+1、涨跌停和交易费用逻辑已验证。
- 空格推进：单次只推进一根，输入框聚焦时不误触。
- 手机响应式：390×844 视口下主要区域可用。

浏览器自动化视觉回归在当前调试环境中不可用，因此后续如果修改图表或移动端布局，应在真实浏览器中再做一次视觉检查。

### 2026-09-11：Windows 浏览器启动修复

- 修复启动器先调用浏览器、服务尚未监听时可能出现空响应的问题。
- 修复无控制台 PyInstaller 版本中 Python 默认请求日志写入不存在的 `stderr`，导致浏览器收到 `ERR_EMPTY_RESPONSE` 的问题。
- 新版启动流程为：创建服务 → 后台监听 → 端口健康检查 → 异步打开浏览器。
- 新版 EXE 回归通过：首页、`/health` 和 `sample_bank.json` 均可访问，样本数 120、形态数 40。

### 2026-09-11：图表布局、缩放和样本训练修复

- 图表标题、身份显示、指标选择和缩放控件改为独立工具条，避免在窄窗口中被压成竖排文字并挤压图表。
- 图表画布固定尺寸，缩放只改变可见 K 线数量；放大显示蜡烛，缩小到高密度时自动显示收盘价走势线。
- 训练结束（包括点击结束和推进到最后一根）会自动展示完整训练片段，并把过密片段切换为走势线，避免图形缩成一团。
- 样本库训练增加快照数据校验、错误提示和状态重置；60 根及以上的完整训练片段可以直接训练，整段行情导入仍保留 70 根最低数据质量门槛。
- 新版独立程序：`发布-浏览器版/K线训练-修复版-v2.exe`；压缩包：`K线训练-Windows-浏览器版-修复版-v2.zip`。

### 2026-09-11：交易按钮与空格快捷键冲突修复

- 修复买入/卖出按钮保持焦点时按空格会触发按钮默认 `click`，导致重复交易的问题。
- 空格推进逻辑现在会阻止按钮默认点击并只推进一根 K 线；输入框、下拉框和复选框仍保留原生空格操作。

### 2026-09-11：形态训练切片逻辑修复

- 训练样本拆分为“训练前背景区”和“形态训练区”两部分。
- 背景区默认保留训练开始前 40 根真实 K 线，仅用于观察大趋势，不计入训练进度，也不能交易。
- 训练区从形态的第 1 根 K 线开始，初始不再直接显示第 41 根；每次点击下一根或按空格只推进 1 根训练 K 线。
- 图表会在背景区与训练区之间绘制“训练开始”分界线。
- 样本生成脚本已改为保存前置 40 根真实行情及 `trainingStartIndex`/`trainingEndIndex`，用于避免训练开始时泄露完整形态。
- 现有 `sample_bank.json` 仍是旧版快照；本次尝试重新从东方财富批量补取背景时遭遇上游连接中断，因此没有用虚拟数据覆盖。旧样本可按新逻辑从第 1 根训练 K 线开始，但需要重新成功获取真实行情后才会拥有前置背景区。

## 10. 当前限制与不能误解的地方

1. 东方财富接口可能限流、断开或受网络环境影响；失败时应用会明确提示，不会伪造数据。
2. 当前实时数据是按需获取，不是每日收盘自动更新。
3. 当前样本库 120 条全部待审核，不能称为 120 条正式训练题。
4. 候选评分是启发式分数，不是经过统计回测证明的置信概率。
5. 筹码峰是量价估算，不等同于真实持仓成本分布。
6. 当前登录是本机昵称登录，不是安全账号系统。
7. 用户数据主要存储在浏览器本地，清理浏览器数据可能导致记录丢失；重要训练应导出 JSON 备份。
8. 当前日线交易实现优先；5 分钟 ETF 单日 T+0 需求尚未完整落地。
9. 多人房间、统一随机分配、实时同步、服务端结算和排行榜尚未实现。
10. GitHub 仓库公开不等于已经获得真实行情数据的商业或公开分发授权。

## 11. 下一阶段建议清单

以下内容按优先级排列，尚未全部实施：

### 优先级 A：完善单人训练

- 建立人工审核工作台和审核结果统计。
- 对样本做证券、日期区间、形态和数据版本去重。
- 为每种形态记录“已审核/待审核/驳回”覆盖率。
- 增加训练难度：标准形态、变形、失败突破、诱多/诱空。
- 增加基准涨跌幅、盈亏比、资金曲线和买卖点复盘。
- 增加每日收盘更新、数据版本冻结、失败重试和更新日志。

### 优先级 B：完善 ETF 分时训练

- 接入独立的 5 分钟数据源。
- 先实现单个交易日，不跨夜持仓。
- 仅对 ETF 开启 T+0 回转交易。
- 明确分时数据授权、缓存、时间边界和停牌处理。

### 优先级 C：完善公网使用

- 将本地昵称升级为匿名用户 ID。
- 增加服务端保存训练记录和样本审核状态。
- 增加权限、限流、日志和异常监控。
- 在公开分享前确认数据源授权及平台费用。

### 优先级 D：多人远程 PK

- 房间码和 2—10 人加入。
- 服务端统一随机标的、起点、终点和数据版本。
- 客户端不能提前获得未来 K 线。
- 服务器统一计算成交、费用、排名和断线重连。

## 12. 后续调用提示词

后续让开发者继续工作时，可以直接使用类似表述：

> 请先阅读 `kline-training.md`、`测试记录.md` 和相关代码，在不破坏现有真实数据、样本审核、交易规则和备份逻辑的前提下，实现下一项需求。完成后按项目约定执行调试，并更新 `kline-training.md` 和 `测试记录.md`。

## 13. 相关文件

- [README.md](README.md)
- [产品需求说明书-v1.md](产品需求说明书-v1.md)
- [测试记录.md](测试记录.md)
- [阶段优化建议.md](阶段优化建议.md)
- [打包说明.md](打包说明.md)
- [公网部署说明.md](公网部署说明.md)
- [sample_bank.json](sample_bank.json)
- [library.js](library.js)
- [server.py](server.py)
- [app.js](app.js)
