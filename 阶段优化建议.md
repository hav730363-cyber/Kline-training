# K线形态训练 App 阶段优化建议

## 下一阶段优先级

1. 建立“真实行情片段库”。每条样本保存证券代码、起止日期、周期、前复权口径、形态标签、标注者和审核状态。这是让随机训练、答案揭晓和多人共享真正可靠的基础。
2. 增加样本审核工作台。用户收藏先进入个人待审核区；至少经过人工确认后才进入公共训练库，避免错误形态污染题库。
3. 完善训练复盘。结束后增加总收益、最大回撤、交易次数、胜率、盈亏比、基准涨跌幅，以及买卖点与形态关键位置的对照。
4. 自动保存未完成训练。刷新页面或意外关闭后可以继续，避免一局训练记录丢失。
5. 增加量能参数设置。默认保留 VOL-MA120/250，同时允许高级用户切换 5/10/20/60/120/250，并说明“天量/地量”的统计窗口。

## 第二优先级

1. 形态难度分级：标准、变形、失败突破、诱多/诱空四类，防止训练库只包含教科书式形态。
2. 训练防泄题：训练中隐藏名称、日期、代码、样本编号和答案标签；结束后统一揭晓。
3. 收藏去重：同一证券、同一时间段、同一形态只保留一条，并记录收藏人数与审核结论。
4. 手机操作：左右滑动查看历史、双指缩放、固定买卖栏，避免小屏频繁滚动。
5. 性能优化：行情和指标计算移至后台线程，保证长周期和 5 分钟数据下仍能流畅推进。

## 多人共享前必须确定

1. 谁能审核公共样本，以及多少票通过才进入训练库。
2. 用户上传或标注错误时的撤回、申诉和版本记录。
3. 行情数据源的授权范围、更新频率、复权方式和异常数据修复规则。
4. 公共排行榜是否按绝对收益、最大回撤调整后收益，或两者同时展示。

## 本阶段补充建议

1. 真实行情先采用“按需获取 + 本地缓存”，确认数据口径稳定后再做每日收盘自动更新；每局训练固定行情版本，避免同一题目因复权或补数据发生变化。
2. “筹码峰”继续标注为估算值。正式版本应记录换手率、流通股本和复权因子，否则不应把它用于自动判定形态。
3. 自动入库建议拆成两道门槛：先验证底部逻辑，再验证形态匹配；只有两者都达到 90% 以上且在回测样本中稳定，才自动入训练库，其余进入审核队列。
4. 当前用户名登录只适合自用测试。多人 PK 前应升级为匿名账号 ID、房间权限、服务端交易结算和断线重连。
5. 形态缩略图适合快速参考，但不能替代样本答案；训练中应隐藏标签，结束后同时显示原始行情、形态示意图和关键突破位置。
