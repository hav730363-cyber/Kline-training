# K线形态训练 App 测试记录

## 固定交付流程

每个阶段完成后执行以下检查：

1. JavaScript、Python 等代码的语法和静态检查。
2. 本地服务和主要页面可访问性检查。
3. 核心用户流程的实际操作测试。
4. 交易资金、持仓、费用和交易限制的边界测试。
5. 桌面端和手机端的响应式布局检查。
6. 浏览器控制台错误检查。
7. 修复确定性问题后重新验证。

对实现方式有九成以上把握的问题直接修复；涉及交易规则、数据口径或产品取舍且把握不足的问题，先形成待确认清单。

## 2026-09-10：日线 MVP 与形态模板库

### 已通过

- `app.js` JavaScript 语法检查。
- `library.js` JavaScript 语法检查。
- 本地服务返回 HTTP 200。
- 初始本金为 1,000,000 元。
- 买入成功后现金和持仓正确变化。
- 当日买入数量不可卖，推进一根日 K 后变为可卖。
- 卖出后持仓归零，交易记录累计为两笔。
- 重新开始后资金、持仓、交易记录和进度恢复初始状态。
- 非 100 股整数倍数量被拒绝，且不会产生交易记录。
- 样本库显示 40 个形态模板。
- 默认训练库显示 22 个形态模板。
- 名称搜索、库切换和随机抽取可用。
- 训练到 180/180 时显示完成状态并禁用“下一根 K 线”。
- 重新开始后完成遮罩消失。
- 390×844 手机视口下，账户卡片、K 线、交易区和筛选区正常响应。
- 浏览器控制台无错误。

### 本轮发现并修复

1. 完成遮罩受到 CSS 样式影响，在训练尚未结束时也覆盖 K 线。已为 `[hidden]` 状态增加明确隐藏规则。
2. 历史不足 60 根时，MA60 被显示成 `¥0.00`。已改为 `—`，避免误导。

### 下一阶段需验证

- 使用真实 A 股和 ETF CSV 文件进行完整导入测试。
- 行情中的停牌、除权除息、重复日期、缺失成交量和异常价格处理。
- 真实手续费规则及其版本化配置。
- 从真实历史行情生成候选波段，并审核进入训练库。

## 2026-09-10：快捷仓位

### 已通过

- 新增买入快捷仓位：全仓、半仓、三分之一仓、六分之一仓。
- 新增卖出快捷仓位：全仓、半仓、三分之一仓、六分之一仓。
- 买入仓位按可用现金和手续费倒算，并向下取整到 100 股。
- 卖出仓位按可卖持仓计算，并向下取整到 100 股。
- 快捷按钮只填写数量，不会直接成交。
- 全仓买入后仍保留支付手续费所需现金。
- 当日买入数量不能通过快捷卖出绕过 T+1。
- 三分之一买入：1,000,000 元演示账户填写 24,000 股。
- 推进一天后三分之一卖出：24,000 股持仓填写 8,000 股。
- JavaScript 语法检查通过，浏览器控制台无错误。

### 本轮发现并修复

1. 使用近似小数计算三分之一仓时，72,000 股可能错误取整为 23,900 股。已改为整数除数 `1/2/3/6` 计算，复验后为 24,000 股。

## 2026-09-10：空格键推进 K 线

- 空格键可推进下一根 K 线。
- 长按空格不会因键盘重复事件连续推进。
- 输入框、搜索框、下拉框、按钮及可编辑区域获得焦点时不响应全局空格快捷键。
- 页面阻止空格的默认滚动行为，避免推进 K 线时页面同时向下滚动。
- 实际页面验证：训练进度由 `41 / 180` 前进到 `42 / 180`，单次按键只推进一根。
- 实际页面验证：交易数量输入框获得焦点时按空格，训练进度保持 `42 / 180`，未发生误触。
- 浏览器控制台错误检查：无错误。

## 2026-09-10：量能均线、指标附图与训练收藏

### 已通过

- 成交量区域显示 VOL-MA120 和 VOL-MA250 两条均线。
- 指标读数同时显示当前成交量、两条量均线和相对 VOL-MA120 的倍数。
- 量能文字状态可区分近 20 日天量/地量、明显放量/缩量及正常量能。
- MACD 附图显示 DIF、DEA 和红绿柱；KDJ 附图显示 K、D、J 三条线。
- 默认同时显示 MACD 与 KDJ，也可单独选择或关闭。
- 演示行情使用 250 根隐藏前置数据，训练从第 41/180 根开始时即可计算两条长期量均线。
- 训练推进至 180/180 后自动揭晓演示样本答案“矩形底”，并禁用继续推进。
- 结束后图表自动从最近 65 根切换为完整 180 根走势，便于对照答案复盘。
- “收藏到训练库”点击后变为“已收藏”，训练片段数量由 0 变为 1。
- 在同源新页面中确认收藏记录仍存在，证明浏览器本地持久化有效。
- “随机选择知识模板”会随机选择知识卡，页面明确提示它不会改变当前行情。
- JavaScript 语法检查通过，本地服务返回 HTTP 200，浏览器控制台无错误。

### 本轮发现并修复

1. 结束后若仍只显示最近 65 根 K 线，无法结合完整走势核对形态答案。已改为训练结束后自动显示完整 180 根。
2. 形态知识模板与实际行情样本概念容易混淆。已重新命名并补充说明；样本答案只来自行情自身的形态标签，不再把随机知识卡当作行情答案。

### 当前限制

- 演示样本为带标签的合成行情；真实 A 股/ETF 样本需在行情接入和人工审核后才能提供可靠答案。
- 收藏记录当前只保存在本机浏览器并标记为待审核；多人共享需等待账号、服务器和审核流程上线。

## 2026-09-10：直观指标布局、估算筹码峰与形态简图

### 已通过

- 主图、成交量、MACD 和 KDJ 按参考布局纵向排列，副图顶部直接显示当前指标数值。
- 主图右侧显示横向估算筹码分布，主峰使用黄色突出，当前价使用红色虚线标记。
- 指标读数显示主筹码峰、估算平均成本、获利比例及 70% 筹码区间。
- “估算筹码峰”开关可以正常隐藏和恢复筹码分布，关闭后主图自动恢复完整宽度。
- 页面两处明确注明筹码峰为量价估算，避免误认为真实持仓成本数据。
- 40 个形态模板全部存在对应的 SVG 简化示意图，无缺失映射。
- 训练库 22 张卡片和完整样本库 40 张卡片均正常显示形态名称、示意图、方向和说明。
- JavaScript 语法检查通过；页面引用的 HTML 元素 ID 无缺失；浏览器控制台无错误。

### 计算口径

- 筹码峰使用当前可见走势的日内高低价区间分摊成交量，并对较早数据进行时间衰减。
- 该口径适合训练时观察成交密集区，不等同于使用真实换手率和流通盘变化计算的券商筹码分布。

## 2026-09-10：真实日线接入与自用测试登录

### 已通过

- `server.py` Python 语法检查通过。
- 本地服务可访问静态页面和 `/api/market/daily` 接口。
- 输入 `600000` 后服务端规范化为 `sh600000`，实际返回 641 根有效前复权日线。
- 页面获取成功后显示真实标的、数据源、复权方式、获取时间和数据根数，并自动重置资金、持仓和训练进度。
- 真实行情仍保持裸 K 模式的名称/日期隐藏规则；勾选后可显示 `sh600000 · 真实行情`。
- 输入格式错误时显示明确的获取失败提示，不会覆盖当前已有训练行情。
- 用户名登录可保存“测试用户”，页面显示“用户：测试用户”；用户名仅存储于当前浏览器。
- 真实行情加载后，筹码峰、VOL-MA120/VOL-MA250、MACD、KDJ 和形态示意图仍正常存在。

### 当前限制与处理

- 当前真实数据为“按需获取”，尚未实现每日收盘自动更新、服务端数据库和数据版本冻结。
- 真实行情没有可靠的自动形态识别器时，用户收藏的样本继续标记为“待审核”；待后续建立可回测的识别规则后，再启用“置信度 ≥90% 自动入训练库”。
- 当前用户名登录是本机测试登录，不提供账号安全、跨设备同步或多人 PK 身份认证。

## 2026-09-10：真实行情候选样本初筛

### 已通过

- 新增“扫描候选片段”按钮，演示行情不会误触发真实候选扫描。
- 真实行情或本地 CSV 可按 60/90/120 根日线窗口扫描，综合下跌、修复、底部位置、量能收缩、反弹动能和形态初筛评分。
- 候选结果显示形态、标的、起止日期、初筛置信度和审核状态，并支持“采用此形态”作为当前训练后的标注模板。
- 候选结果保存于当前浏览器；切换标的或导入新文件时会清空旧候选，避免不同数据集混用。
- 规则引擎设置了明确的回测开关：未完成验证前，即使初筛分数达到 90%，也只显示“待审核”。
- JavaScript 语法检查、本地服务 HTTP 200 和页面元素引用检查通过。
- 使用 `sh600000` 实际扫描得到 40 个候选片段，候选卡片可正常显示并可“采用此形态”。
- 当前规则未开启自动入库开关，扫描提示明确显示“尚未完成回测”，避免把启发式分数误称为真实概率。

## 2026-09-10：候选审核与独立训练区间

### 已通过

- 候选卡片新增“审核通过并入训练库”和“驳回”，审核状态持久化在当前浏览器。
- 审核通过后候选库计数正确增加，并显示审核人和审核时间。
- “训练此片段”会把训练起止索引切换为候选片段的独立区间，不再使用整段行情；实测区间显示为 `41 / 90`、`41 / 120` 等对应窗口长度。
- 独立训练仍隐藏名称和日期，重置后回到该候选区间起点，交易资金重新回到 1,000,000 元。
- 修复新扫描候选缺少 `reviewStatus` 导致审核按钮不显示的问题，并完成复验。

### 当前限制

- 这是可解释的初筛器，不是经过统计验证的自动识别模型；暂不宣称初筛分数等于真实概率。
- 当前候选片段先展示和标注，尚未实现点击后截取独立训练区间；该功能将在下一步训练场景数据结构中加入。
## 2026-09-11：第 1—11 步基础优化回归

### 已通过

- `python -m py_compile server.py desktop_launcher.py` 通过。
- `node --check app.js`、`node --check library.js` 通过。
- 页面新增控件均已存在且 HTML `id` 无重复：周期选择、缩放、结束训练、随机抽题、备份导入导出和复盘统计。
- 本地 `/health` 返回 HTTP 200；非法代码返回 HTTP 400，未覆盖现有行情。
- 服务端真实行情解析单元测试通过：东方财富响应可解析为 OHLCV，返回前复权、周期、有效数量、异常/重复数量和更新时间元数据。
- 真实行情接口失败时返回明确错误；当前网络环境对东方财富请求出现上游限流/SSL 关闭，前端不会回退到合成数据。
- 候选样本改为跨行情累积去重，随机训练仅抽取当前标的的已审核真实样本；未满足条件时明确提示先扫描并审核。
- 交易规则增加 A 股 T+1、ETF T+1、ETF T+0、佣金最低收费、A 股卖出印花税、过户费、停牌/风险标的和涨跌停封板校验。
- 增加训练草稿自动保存、JSON 备份导入导出、最大回撤/胜率/手续费/已实现盈亏复盘统计。

### 当前限制

- 东方财富接口当前被网络环境限流，暂时不能在本次调试中批量下载真实样本；代码明确禁止用假数据填充样本库。
- “每种形态 3 份”目前以 `已审核真实样本/目标样本数` 显示覆盖率，只有真实行情成功获取并经过审核后才计入，尚不能诚实宣称已完成 120 份样本。
- 当前页面仍是本地/单机数据模型；跨用户共享训练库和交互 PK 继续按用户要求暂缓。

## 2026-09-11：真实样本库建立与 EXE 回归

### 已通过

- 东方财富备用主机真实采集完成：`sample_bank.json` 共 120 条候选，40 个形态每个恰好 3 条。
- 每条样本均包含真实 OHLCV 快照、证券代码、起止日期、东方财富来源、前复权口径和待审核状态；不存在合成行情样本。
- 样本库 JSON 可由网页端自动载入，并支持从快照开始训练；随机抽题只允许已审核真实样本。
- 新版 EXE 已重新打包，内置 `sample_bank.json`；启动后 `/health` 返回 200，样本库返回 200，报告 40 个形态、120 条样本。
- 调试时发现旧版测试进程占用 18000；使用独立端口复验新版 EXE，已确认桌面服务正常，诊断版已清理，正式 spec 恢复无控制台窗口。

### 当前限制

- 120 条样本目前全部是“待审核”，因为启发式形态评分未经标注集回测，不能直接冒充 90% 置信度；审核通过后才会进入随机训练池。
- 东方财富证券列表接口偶发断开，本次采用预设的非 ST/退市代码并逐只通过东方财富 K 线响应校验；部分证券名称缺失时页面显示代码，不影响行情真实性。

## 2026-09-11：Windows 浏览器版空响应修复

### 问题

- 旧版 Windows 无控制台 EXE 启动后，浏览器可能显示 `ERR_EMPTY_RESPONSE`。
- 诊断确认：服务端默认请求日志写入不存在的 `sys.stderr`，导致响应尚未发送就关闭连接；旧启动顺序也可能在服务监听前调用浏览器。

### 修复

- `server.py` 为请求日志增加无 `stderr` 环境兼容处理。
- `desktop_launcher.py` 改为先启动服务、等待端口健康检查通过，再异步打开浏览器。
- 增加启动失败阶段日志和 Windows 错误提示。
- 新增 `启动K线训练.cmd`，支持从压缩包目录双击启动。
- 新建独立浏览器版打包配置，避免覆盖仍被旧进程占用的旧 EXE。

### 回归结果

- 新版无控制台 EXE 启动成功。
- 首页返回正常，`/health` 返回 HTTP 200。
- `sample_bank.json` 返回正常：40 种形态、120 条样本。
- 交付压缩包 `K线训练-Windows-浏览器版.zip` 实测通过。

## 2026-09-11：缩放后 K 线消失修复

### 问题

- 缩放到约 35 根 K 线时，页面仍显示缩放数量和图例，但 K 线绘制区域可能只剩空白或单条竖线。

### 修复

- 画布尺寸优先读取自身和父容器的有效尺寸，避免布局刷新时使用 0 宽度。
- 每次重绘使用 `setTransform` 重置高 DPI 变换，避免多次缩放后的坐标累积。
- 缩放和恢复全景改为等待下一帧布局完成后重绘。
- 可见数量严格限制在当前有效 K 线范围内，并限制绘图区最小宽度。
- 增加图表面板的 `min-width: 0`，避免响应式布局挤压画布。

### 静态回归

- `node --check app.js` 通过。
- `node --check library.js` 通过。
- `git diff --check` 通过。

## 2026-09-11：图表布局、自由缩放和样本训练回归

### 已通过

- `node --check app.js`、`python -m py_compile server.py desktop_launcher.py` 通过。
- `git diff --check` 通过；HTML、CSS 静态检查确认图表标题工具条、600px 固定画布和换行规则均已生效。
- 图表绘制增加空数据保护、有效尺寸保护、高密度走势线模式和缩放边界按钮状态。
- 点击结束训练与推进到最后一根 K 线都统一进入完整复盘视图，不再保留缩放中的异常窄视图。
- 样本快照训练改为独立最低长度校验；样本库中的 60/90/120/180 根片段均满足训练入口要求，旧的整段行情 70 根质量门槛未降低。
- 新版 EXE `K线训练-修复版-v2.exe` 启动成功；独立端口 `/health` 返回 200，首页、`app.js`、`styles.css` 和 `sample_bank.json` 均可访问，样本数 120。

### 说明

- 当前环境的浏览器自动化视觉回归仍不可用，因此本次完成了代码级布局修复和打包服务回归；打开新版程序后应优先观察图表工具条是否横向排列、缩放到最小时是否出现“走势线”提示，以及点击“训练此片段”后进度是否重置。

## 2026-09-11：交易按钮与空格快捷键冲突修复

### 已通过

- 空格快捷键现在会阻止按钮获得焦点时的浏览器默认 `click` 行为，避免买入或卖出后再次按空格重复提交同一笔交易。
- 输入框、下拉框和复选框仍保留原生空格行为，不影响编辑数量或切换选项。
- `node --check app.js`、Python 编译检查和 `git diff --check` 通过。

## 2026-09-11：微信小程序测试骨架检查

### 已通过

- AppID `wx709f67396b06ba71` 已写入 `miniprogram/project.config.json`。
- 小程序配置文件和 CloudBase 云函数 `package.json` 均可正常解析。
- `miniprogram/app.js`、小程序首页脚本和 `cloudfunctions/getMarketData/index.js` 均通过 `node --check`。
- 云函数只包含东方财富真实行情请求和 OHLCV 校验，不包含 AppSecret 或腾讯云密钥。

### 尚待用户侧操作

- 在微信开发者工具导入 `miniprogram/`。
- 在腾讯云创建测试 CloudBase 环境，填写环境 ID 并上传 `getMarketData` 云函数。
- 使用真实 AppID 进行预览测试；小程序完整指标、交易和样本云同步将在链路通过后迁移。

## 2026-09-11：形态训练切片修复

### 已通过

- `node --check app.js`、`python -m py_compile build_sample_bank.py` 和 `git diff --check` 通过。
- 初始训练位置由训练区第 41 根改为第 1 根；训练进度与背景行情分离。
- 图表支持训练前 40 根背景区，并以“训练开始”分界线区分背景和训练内容。
- 新样本生成脚本会保存 40 根真实前置背景及训练区索引。

### 当前限制

- 当前 `sample_bank.json` 仍为旧版 120 条快照，没有前置背景字段。
- 重新下载 5 个相关股票的真实历史行情时，东方财富连接被远端关闭；未使用虚拟数据填充。
- 待网络恢复后应重新生成样本库，并检查每条样本满足：背景 40 根、训练区从索引 40 开始、训练区长度至少 60 根。
