# 微信小程序测试版

小程序 AppID：`wx709f67396b06ba71`

## 当前范围

这是第一阶段链路测试骨架：

- 微信开发者工具可以导入；
- 使用微信身份作为用户身份；
- 通过 CloudBase 云函数访问东方财富真实行情；
- 前端先绘制收盘价走势线；
- 不把 AppSecret、腾讯云密钥或数据源密钥写进代码。

完整指标、模拟交易、样本库同步和多人 PK 会在链路确认后逐步迁移。

## 测试步骤

1. 安装并打开微信开发者工具。
2. 导入 `miniprogram` 目录，AppID 使用项目内已配置的 AppID。
3. 在腾讯云 CloudBase 创建测试环境，建议环境名使用 `kline-training-test`。
4. 在 `miniprogram/app.js` 的 `CLOUD_ENV_ID` 填入 CloudBase 环境 ID；也可以在调试时写入本地缓存键 `kline-cloud-env-id`。
5. 在开发者工具中上传 `cloudfunctions/getMarketData` 云函数。
6. 点击小程序中的“获取东方财富行情”，输入例如 `600000` 测试。

## 费用建议

测试阶段先使用 CloudBase 免费体验环境，不开启按量付费。确认链路和实际用量后，再决定是否升级套餐。

## 重要限制

- 东方财富接口可能限流、拒绝请求或调整接口；失败时页面应显示错误，不使用合成行情替代。
- 正式给普通用户使用前，需要完成小程序账号主体、隐私协议、类目和版本审核等配置。
- 后续样本库和训练结果应放在云数据库，不能只放在小程序本地缓存。
